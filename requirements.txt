langchain-text-splitters
langchain-core
langchain-community
langchain
pydantic-settings
# ========== Web 框架 ==========
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.22

# ========== 大语言模型（阿里 Qwen） ==========
# 官方推荐使用 dashscope SDK
dashscope==1.14.1
# 或者使用 OpenAI 兼容接口（如本地部署的 Qwen），可选
# openai==1.3.0

# ========== 语音识别（Whisper） ==========
openai-whisper==20231117
# 如需使用 OpenAI API 版本的 Whisper，可添加：
# openai==1.3.0

# ========== 向量数据库 ==========
chromadb==0.4.22

# ========== 文本嵌入模型 ==========
# text2vec 和 BAAI/bge-large-zh 均基于 sentence-transformers
sentence-transformers==2.2.2
# 如需单独安装 text2vec（可选，sentence-transformers 已包含相似功能）
# text2vec==1.0.6

# ========== 多模态理解 ==========
# CLIP (OpenAI 官方实现)
git+https://github.com/openai/CLIP.git
# 或使用 transformers 版本的 CLIP（推荐，更稳定）
transformers==4.41.2
# BLIP 依赖 transformers
# Pillow 图像处理
pillow==12.1.1

# ========== 文字识别（PaddleOCR） ==========
paddleocr==2.7.3
# PaddlePaddle 需根据 CUDA 版本选择，CPU 版本如下：
paddlepaddle==2.6.2
# 如有 GPU（CUDA 11.7），请改为：
# paddlepaddle-gpu==2.6.1

# ========== 视频处理 ==========
opencv-python==4.8.1.78

# ========== 课件生成 ==========
python-pptx==0.6.23
python-docx==1.1.0

# ========== 辅助工具 ==========
# 环境变量管理
python-dotenv==1.0.0
# 日志
loguru==0.7.2
# 文件类型检测
python-magic==0.4.27
# 异步 HTTP 客户端（调用外部 API 时使用）
httpx==0.25.1
# 数据处理
numpy==1.24.3
# 图像处理（补充）
opencv-python-headless==4.8.1.78  # 可选，无 GUI 版本